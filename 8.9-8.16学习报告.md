# 学习报告

郑俊灏

注：8.12和8.16出去了，没怎么看，内容有点乱基本上是随笔，可能有错误理解后续及时补正。

**2026.8.9**

## 2026.8.9
**学习对象：** `MAS_VISION_MIAN/rm_utils/extend_kalman_filter.cpp`

**学习内容：**  
EKF主要包含先验预测与更新，而预测和更新分为线性和非线性，根据给不同的计算方式可以切换线性与非线性的卡尔曼滤波算法。

---

## 2026.8.10
**学习对象：** `MAS_VISION_MIAN/auto_aim/armor_dector/number_classifier.cpp`

**学习内容：**  
1. ONNX模型是一种模型格式，需要输入.onnx文件的模型地址和标签文件。  
2. `k_extend_ratio`是把灯条的方向向量映射到装甲板一半的系数，方便根据灯条中心加减半个装甲板方向向量确定上下角点。  
3. 图像透视转换：  
   1）源点`src_vertices`：获取图像的四个角点。  
   2）目标点`dst_vertices`：根据大小装甲板预先确定的中间处理时的矩形区域（比模型输入稍微大一点，方便裁剪中间数字区域）。  
   > 注：在像素处理时由于原点为（0,0），故为达到预期坐标需要减1。  
   3）透视转换函数：  
   - `getPerspectiveTransform()` 根据两组点`src_vertices`和`dst_vertices`计算出一个 3x3 的变换矩阵。  
   - `warpPerspective()` 用这个矩阵对原图 `src` 进行变形，生成一张 `warp_width × warp_height` 的矫正图 `number_image`。  
4. 裁切调整为20*28数字尺寸的roi区域后还要拉伸为模型输入的28*28尺寸，对模型识别影响不大。  
5. 神经网络训练：  
   1）图像归一化。  
   2）创建并根据图像生成图像训练标准格式`blob[批次=1, 通道数=1, 高=28, 宽=28]`。  
   3）模型推理：  
   - `setInput()`：将 Blob 设置到网络的输入层。  
   - `forward()`：执行前向传播，进行推理，输出结果`outputs`（对于每个对象标签的评分/相似程度）。  
   4）解码输出：  
   - `outputs.reshape(1, 1)`：把`outputs`矩阵变为`[1, n]`的矩阵形式。  
   - `minMaxLoc()`函数找这里面的最大值（即置信度）与最大值的位置（即索引的对象）。  
6. 检测无效结果（置信度小于阈值的、忽略列表里的、装甲板id与大小不匹配的）。

**问题/未解决事项：**  
- `mas_log`的具体操作方式。

---

## 2026.8.11
**学习对象：** `MAS_VISION_MIAN/armor_shoot/armor_shoot.cpp`

**学习内容：**  
1. `57.3`为角度转弧度系数。  
2. `what()`是`std::exception`类的虚函数，返回异常的错误描述。  
3. `AimPoint{false, {}}`赋值逻辑是把第二位赋值为空，第一位占位赋值0，其余默认赋值0。  
4. 计算chrono时间差时一般用微秒`std::chrono::microseconds(static_cast<int>(dt * 1e6))`表示，用浮点秒`std::chrono::duration<double>(dt)`会出现累积误差。  
5. 选择点首先排除无数据和不在范围内的id，然后在没有跳帧的情况下锁定原来的目标，有跳变的时候分为三种情况：  
   1）高速陀螺和中速英雄情况，不锁定，找一个角度合适的（即装甲板相对旋转中心对于摄像头偏移角度`delta_angles`小的）就可以开火。  
   2）低速或静止目标（跟踪），锁定一个板（低速好瞄准），大于1则比较前两个目标小的即为锁定目标*，小于2且不是原来的那个才更新，没数据就找默认（上一帧第一位）数据。  
   3）中速目标首先根据旋转方向与装甲板相对中心位置的方位选择一个正在靠近中心位置的装甲板确定为目标，没有先用锁定，第三用角度小的，最后没有对象的用默认。  
6. 弹道解算：  
   1）`Trajectory`对象传入子弹初速度`bullet_speed_`，水平投影距离`d0`，目标高（Z）`xyz0[2]`，传出初始预测飞行时间`trajectory0.fly_time`和枪口高度`iteration_target[iter].predict(predict_time)`。  
   2）在计算出现在位置弹道后，要根据现在的飞行时间通过EKF扩展卡尔曼预测中目标位置，并根据位置反向求解新的飞行时间，根据新的时间再用EKF预测位置，迭代至多10次（创立10个副本*），直到时间收敛到0.001内。  
   3）排除转向角度过大和将反向角度归一化操作后，计算yaw和pitch角（要加相机和云台之间的机械误差）以及目标到枪口真实水平距离dist，再根据远精近松的原则确定云台角度偏差的容忍程度，保证不转到位不开火。

**问题：**  
1. 低速判定逻辑在大于1个装甲板直接取前两个数据比较而忽略第三个，或许存在三个装甲板出现而第三个为最佳目标情况，可优化为进一步判定第三位数据，建议使用中速的判定方法。  
2. 低速情况下代码以保持连续性为主，但上次实测过程中发弹并不连贯（弹道感觉不可能无解），与代码逻辑矛盾，是否存在目标丢失问题，是否为`armor_dector`识别逻辑有问题或者模型置信度阈值过高带来的目标丢失时间过长。

---

## 2026.8.12
跟同学出去玩了

---

## 2026.8.13
**学习对象：** `MAS_VISION_MIAN/armor_track/armor_pose.cpp`

**学习内容：**  
1. 坐标系转换需要先提取imu姿态，将四元数转化为矩阵`R_imubody2world`后，用云台转imu的`R_gimbal2imubody_`来乘（从右至左），获得云台在世界的姿态，并通过转置矩阵`R_gimbal2imubody_.transpose()`转换为云台自己的语言。  
2. 要获得装甲板对于世界和云台的坐标，需要先用`solvepnp`解算出旋转向量`rvec`和平移向量`tvec`，然后先将二者分别转换成相机坐标系下的平移和旋转矩阵，然后再转换成云台视角下世界坐标系中的矩阵`armor.xyz_in_world`和`R_armor2world`。  
   > 注：1.中对于坐标系姿态转换需要乘转置矩阵，是因为`R_imubody2world`为imu视角下的矩阵，计算出的世界坐标系下的姿态也是imu的，需要转换成云台视角；而2.中计算出的世界坐标系中的视角为云台视角，因此不需要转置。  
3. 获得的`armor.ypr_in_world`是装甲板对于世界的位姿，`armor.ypd_in_world`是装甲板在世界中对于云台的方向和坐标。

**问题：**  
- `armor_pose.cpp`中平衡优化`is_balance`对于5号装甲板依然判定为平衡步兵，建议删除以防止后续赛规改革。

---

## 2026.8.14
**学习对象：** `MAS_VISION_MIAN/armor_track/armor_pose.cpp`

**学习内容：**  
1. Yaw轴精细搜索`optimize_yaw`通过循环140次，将三维装甲板映射到二维然后跟识别的图像做角点误差分析，找出最小误差点。  
2. 投影过程是将装甲板坐标系转到相机坐标系，构建相机坐标系下的三维装甲板，并通过`cv::projectPoints`映射到二维获取角点，并依次跟原角点取距离得误差值。

**问题：**  
1. 140次循环计算量有点大，建议可以换成梯度下降法。  
2. 前哨站装甲板的优化中重复调用了一次`solvepnp`，为了获取原始数据建议在最初获取`solvepnp`时保留一份副本。  
3. 对于前哨站装甲板优化中直接调用pnp的yaw值作为yaw，精度不如优化后的。  
4. 代码封装有些混乱，建议把投影函数`reproject_armor`改成通用方式，去除`outpost_reprojection_error`中的`solvepnp`和投影方式。  
5. 本代码中的`cv::norm`并未广泛应用，建议统一计算格式，增加可读性。

**未解决事项：**  
1. 未知上交大的损失计算方法`SJTU_cost`。  
2. 未知装甲板安放策略（前哨站 -15度，其他 15度）：  
   ```cpp
   auto pitch = (name == "outpost") ? -15.0 * CV_PI / 180.0 : 15.0 * CV_PI / 180.0;

**8.15补记：**
由于在理解过程中ai翻译问题，现把文中错误的“哨兵”改为“前哨站”（原谅我没仔细看label），并从新进行解读，目前还未发现错误后续第三次看需要着重检查是否有问题

---

## 2026.8.15
**学习对象：** `MAS_VISION_MIAN/armor_track/armor_target.cpp`

**学习内容：**  
1. `asDiagonal()`可以将向量转变为初始协方差矩阵。  
2. `x_add`封装的`Eigen::VectorXd`加法后的yaw归一化，非线性状态转移函数`f`封装了`F*x`后对yaw的归一化，防止角度超出范围。  
3. EKF预测应用主要是创建状态转移矩阵`F`和过程噪声矩阵`Q`并带入EKF预测计算。  
   > 注：当检测到前哨站时，要把角速度限制到官方限制的角速度区间（2.51到-2.51）。  
4. EKF更新的应用主要体现在：  
   1）先要对观测到的矩阵和预测的几个矩阵匹配，按照距离和角度加权（旋转速度大的时候角度加权大）计算出最佳的装甲板即为对应的装甲板。  
   > 注：将`bestid`储存到`lastid`里通过第二次比较可以看出id是否存在变化。  
   2）然后用`h_jacobian`计算雅可比矩阵`H`。  
   3）确定自适应观测噪声`R`。  
   4）从装甲板状态中提取世界坐标并算出`angle`，得到预测量`h`。  
   5）算法`z_subtract`作`Eigen::VectorXd`减法并分别对ypr归一化。  
   6）获取测量值`z`。  
   7）导入EKF更新算法。  
5. 前哨站相对于普通装甲板，区别在于前哨站水平方向排布3个装甲板而不是4个，高度是根据装甲板id每层设置一个，高度跟id有关，不用考虑普通装甲板大装甲板安装向上移动半径增大（`use_lh`）的问题。

**未解决事项：**  
- 未知`F`、`Q`、`H`矩阵怎么写的（为什么这么写）。


## 2026.8.16
**学习对象：** `MAS_VISION_MIAN/armor_track/armor_target.cpp`

**学习内容：**  
对于机甲平移速度有最大值规范`max_linear_vel`（`w*r`），而为了防止`max_linear_vel`太小等于0则作最小0.5的限定。